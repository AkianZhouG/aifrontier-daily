# 安全策略

## 项目边界

AI Frontier Daily 默认只绑定本机地址，且会抓取外部网页、写入本地数据库，并可调用用户配置的智能代理。它适合个人本机使用，不应在没有额外认证、访问控制和隔离的情况下部署到公网。

Pi 的无工具参数可以缩小权限，但 Pi 本身不是沙箱。请只配置自己信任的代理可执行文件和模型提供方。

## 报告安全问题

请不要在公开 Issue、讨论区或 Pull Request 中披露尚未修复的安全问题。

- 如果仓库托管在 GitHub，请使用 Private Vulnerability Reporting 或 Security Advisory。
- 如果仓库托管在其他平台，请通过维护者提供的私密联系方式联系维护者。
- 报告中请说明受影响版本、复现步骤、影响范围和建议修复方向。
- 请删除密钥、认证文件、私有 URL、完整日志和个人数据；如果必须提供样例，请先脱敏。

目前重点关注：

- SSRF、重定向和外部来源请求绕过；
- 本地服务被远程访问或跨站控制；
- 任意命令执行、危险子进程和路径穿越；
- 模型提示词注入导致的权限越界；
- 日志、数据库和生成产物中的敏感信息泄露。
